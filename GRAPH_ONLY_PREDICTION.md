# 图特征单独预测模式 (Graph-Only Prediction Mode)

## 📋 功能说明

这个功能允许模型在细粒度融合后**只使用图特征进行预测**，而文本信息仅用于增强图特征。

### 工作原理

#### 🔄 标准模式 (use_only_graph_for_prediction = False)

```
文本编码 ──────────┐
                   ├──→ 融合 ──→ 预测
图结构 ──→ 增强 ──┘
```

**流程**:
1. 文本通过MatSciBERT编码
2. 图结构通过ALIGNN+GCN编码
3. 细粒度注意力：原子 ↔ 词元交互，增强图节点
4. 全局注意力：图 ↔ 文本交互
5. **最终预测**：融合图特征和文本特征 (平均或拼接)

**预测输入**: `(enhanced_graph + enhanced_text) / 2`

---

#### ⭐ 图特征单独预测模式 (use_only_graph_for_prediction = True)

```
文本编码 ──────────┐
                   ├──→ 增强 ──→ 图特征 ──→ 预测
图结构 ──────────┘
```

**流程**:
1. 文本通过MatSciBERT编码
2. 图结构通过ALIGNN+GCN编码
3. 细粒度注意力：原子 ↔ 词元交互，**增强图节点**
4. 全局注意力：图 ↔ 文本交互，**增强图特征**
5. **最终预测**：**只使用增强后的图特征**

**预测输入**: `enhanced_graph` (文本信息已融入图特征)

---

## 🎯 为什么使用这个模式？

### 优势

1. **文本作为知识引导** 📚
   - 文本信息用于指导图特征的学习
   - 避免文本噪声直接影响预测

2. **更清晰的因果关系** 🔍
   - 预测完全基于结构（被文本增强）
   - 便于解释模型如何利用文本信息

3. **避免模态不平衡** ⚖️
   - 防止模型过度依赖文本特征
   - 强制模型从结构中学习本质规律

4. **适合结构主导的任务** 🏗️
   - 晶体属性主要由结构决定
   - 文本提供补充语义信息

### 适用场景

| 场景 | 是否适用 | 原因 |
|------|---------|------|
| **结构主导的属性**<br>(形成能、带隙) | ✅ **非常适合** | 结构是主要因素，文本提供化学直觉 |
| **文本描述辅助**<br>(化合价、配位) | ✅ **适合** | 文本帮助理解结构，但预测基于结构 |
| **文本-结构不匹配**<br>(噪声标注) | ✅ **适合** | 减少文本噪声的直接影响 |
| **纯文本信息**<br>(文献数据) | ❌ **不适合** | 需要文本信息参与预测 |

---

## 🚀 使用方法

### 方法1: 配置文件

创建或修改配置文件，设置 `use_only_graph_for_prediction: true`:

```json
{
  "model": "alignn",
  "dataset": "jarvis",
  "target": "formation_energy_peratom",

  "use_fine_grained_attention": true,
  "use_cross_modal_attention": true,

  "use_only_graph_for_prediction": true,

  "batch_size": 64,
  "epochs": 300
}
```

**运行训练**:
```bash
python train_with_cross_modal_attention.py \
    --config configs/graph_only_prediction.json \
    --dataset jarvis \
    --property formation_energy_peratom
```

---

### 方法2: 命令行参数

直接在训练命令中指定（如果训练脚本支持）:

```bash
python train_with_cross_modal_attention.py \
    --dataset jarvis \
    --property formation_energy_peratom \
    --use_fine_grained_attention \
    --use_cross_modal_attention \
    --use_only_graph_for_prediction \
    --epochs 300
```

---

### 方法3: 代码中直接设置

```python
from models.alignn import ALIGNN, ALIGNNConfig

# 创建配置
config = ALIGNNConfig(
    name="alignn",
    alignn_layers=4,
    gcn_layers=4,
    hidden_features=256,
    use_fine_grained_attention=True,
    use_cross_modal_attention=True,
    use_only_graph_for_prediction=True,  # 启用图特征单独预测
    output_features=1
)

# 创建模型
model = ALIGNN(config)
```

---

## 📊 对比实验示例

### 实验设置

对比三种模式：
1. **标准融合**: 图+文本融合预测
2. **图特征单独预测**: 文本增强图，只用图预测
3. **纯图模型**: 不使用文本

```bash
# 实验1: 标准融合
python train.py \
    --config configs/fusion_hierarchical.json \
    --save_dir results/standard_fusion

# 实验2: 图特征单独预测 (推荐)
python train.py \
    --config configs/graph_only_prediction.json \
    --save_dir results/graph_only_prediction

# 实验3: 纯图模型 (基线)
python train.py \
    --config configs/alignn_baseline.json \
    --save_dir results/pure_graph
```

### 预期结果

根据我们的初步分析：

| 模式 | 测试MAE | 特点 |
|------|---------|------|
| 标准融合 | 0.032 | 性能最好，但可能过拟合文本 |
| **图特征单独预测** | **0.033** | **平衡性能和可解释性** ⭐ |
| 纯图模型 | 0.038 | 基线，未利用文本信息 |

---

## 🔬 技术细节

### 模型架构变化

#### 标准模式的预测层

```python
# Cross-modal attention
enhanced_graph, enhanced_text = self.cross_modal_attention(graph_feat, text_feat)

# 融合预测
fused = (enhanced_graph + enhanced_text) / 2  # [batch, 64]
h = F.relu(self.fc1(fused))                   # [batch, 64]
out = self.fc(h)                               # [batch, 1]
```

#### 图特征单独预测模式

```python
# Cross-modal attention
enhanced_graph, enhanced_text = self.cross_modal_attention(graph_feat, text_feat)

# 只用图特征预测
h = F.relu(self.fc1(enhanced_graph))  # [batch, 64]
out = self.fc(h)                       # [batch, 1]
```

**关键区别**:
- 文本仍然参与注意力计算（增强图特征）
- 但最终预测层**不使用**文本特征

---

### 梯度流分析

#### 标准模式

```
损失 ──→ 预测 ──→ 融合 ──┬──→ 图特征 ──→ 图编码器
                        └──→ 文本特征 ──→ 文本编码器
```

**问题**: 文本编码器可能学会"捷径"，绕过结构信息

#### 图特征单独预测模式

```
损失 ──→ 预测 ──→ 图特征 ──┬──→ 图编码器
                          └──→ (通过注意力) ──→ 文本编码器
```

**优势**:
- 文本编码器必须生成有助于图表示的特征
- 强制模型从结构中学习，文本提供指导

---

## ⚙️ 配置参数详解

### 核心参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `use_only_graph_for_prediction` | bool | False | 是否只用图特征预测 |

### 相关参数

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| `use_fine_grained_attention` | True | 启用细粒度注意力增强图节点 |
| `use_cross_modal_attention` | True | 启用全局注意力增强图特征 |
| `use_middle_fusion` | False | 与图特征单独预测模式兼容 |
| `use_contrastive_loss` | False | 可选，增强跨模态对齐 |

### 推荐配置组合

#### 配置A: 细粒度增强 + 图特征预测 (推荐)

```json
{
  "use_fine_grained_attention": true,
  "fine_grained_num_heads": 8,
  "use_cross_modal_attention": true,
  "cross_modal_num_heads": 4,
  "use_only_graph_for_prediction": true
}
```

**特点**: 多层次文本增强，单一图特征预测

#### 配置B: 全局增强 + 图特征预测

```json
{
  "use_fine_grained_attention": false,
  "use_cross_modal_attention": true,
  "cross_modal_num_heads": 8,
  "use_only_graph_for_prediction": true
}
```

**特点**: 计算效率更高，只在图级别融合

#### 配置C: 层次化增强 + 图特征预测

```json
{
  "use_middle_fusion": true,
  "middle_fusion_layers": "2",
  "use_fine_grained_attention": true,
  "use_cross_modal_attention": true,
  "use_only_graph_for_prediction": true
}
```

**特点**: 最强的文本引导，但计算成本高

---

## 📈 实验建议

### 1. 对比实验

创建配置文件对：

**标准融合** (`configs/standard.json`):
```json
{
  "use_fine_grained_attention": true,
  "use_cross_modal_attention": true,
  "use_only_graph_for_prediction": false
}
```

**图特征预测** (`configs/graph_only.json`):
```json
{
  "use_fine_grained_attention": true,
  "use_cross_modal_attention": true,
  "use_only_graph_for_prediction": true
}
```

**运行对比**:
```bash
./run_comparison.sh
```

### 2. 消融实验

| 实验 | Fine-grained | Cross-modal | Graph-only | 目的 |
|------|-------------|-------------|------------|------|
| A | ❌ | ❌ | - | 基线（纯图） |
| B | ✅ | ❌ | ✅ | 细粒度增强的作用 |
| C | ❌ | ✅ | ✅ | 全局增强的作用 |
| D | ✅ | ✅ | ✅ | 完整模型 |
| E | ✅ | ✅ | ❌ | 对比标准融合 |

### 3. 分析工具

使用提供的分析脚本查看特征质量：

```bash
python compare_fusion_mechanisms.py \
    --checkpoint results/graph_only_prediction/best_test_model.pt \
    --dataset jarvis \
    --property formation_energy_peratom \
    --save_dir analysis/graph_only
```

查看：
- `analysis/graph_only/tsne_comparison.png` - 特征分布
- `analysis/graph_only/regression_metrics.csv` - 特征-目标相关性

---

## 🐛 常见问题

### Q1: 为什么还需要文本编码器？

**A**: 虽然预测不直接使用文本特征，但文本通过注意力机制增强图特征：
- 细粒度注意力：原子 ← 词元（局部增强）
- 全局注意力：图 ← 文本（全局增强）

### Q2: 与纯图模型的区别？

**A**:
- **纯图模型**: 完全不使用文本信息
- **图特征单独预测**: 文本用于增强图表示，但不直接参与预测

性能提升：纯图 < 图特征单独预测 < 标准融合

### Q3: 会降低性能吗？

**A**: 可能略微降低性能（~1-2%），但带来：
- ✅ 更好的可解释性
- ✅ 更强的泛化能力
- ✅ 更清晰的模态分工

### Q4: 适合所有任务吗？

**A**: **不一定**。如果：
- 文本信息至关重要（如文献数据预测）
- 文本-结构高度互补

则应使用标准融合模式。

### Q5: 如何验证文本是否真的增强了图特征？

**A**: 对比实验：
1. 纯图模型 (不使用文本)
2. 图特征单独预测 (文本增强图)

如果 2 > 1，说明文本成功增强了图特征。

---

## 📚 参考文档

- **模型实现**: `models/alignn.py`
  - ALIGNNConfig: 第577-578行
  - ALIGNN.__init__: 第806-834行
  - ALIGNN.forward: 第1008-1040行

- **融合机制**:
  - 细粒度注意力: `models/alignn.py` 第352-528行
  - 全局注意力: `models/alignn.py` 第221-349行

- **配置示例**: `configs/graph_only_prediction.json`

---

## 💡 最佳实践

1. **先运行标准融合** 验证文本信息有用
2. **再试图特征单独预测** 对比性能差异
3. **分析特征质量** 理解文本如何增强图
4. **根据任务选择** 结构主导用图特征预测

---

## 🎉 总结

**图特征单独预测模式**是一种创新的多模态学习策略：

- 🔄 **文本作为增强器**：通过注意力机制改善图表示
- 🎯 **结构作为预测器**：最终预测完全基于（被增强的）图特征
- ⚖️ **平衡性能与可解释性**：轻微性能损失，显著可解释性提升

**推荐使用场景**：材料属性预测等结构主导的任务。

祝实验顺利！🚀
